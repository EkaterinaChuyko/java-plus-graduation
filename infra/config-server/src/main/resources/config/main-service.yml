server:
  port: 8080

stats:
  url: http://stats-server/hit

spring:
  datasource:
    url: jdbc:postgresql://localhost:6543/ewm
    username: user
    password: pass
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false

  sql:
    init:
      mode: always